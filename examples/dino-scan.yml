# Example: run Dino on every push to main and on PRs.
# Copy this into your repo's .github/workflows/ directory.
#
# Secrets:
#   API_URL    — your deployed API endpoint (run Dino after the API is deployed)
#   API_TOKEN  — bearer token for your API (optional)

name: API Verification

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  dino-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: Dino-HQ/dino/.github/actions/scan@main  # pin to a commit SHA in production
        with:
          api-url: ${{ secrets.API_URL }}
          api-token: ${{ secrets.API_TOKEN }}
          fail-on-high: true   # exit 3 on any HIGH or CRITICAL finding
          # protocol: rest
          # spec-url: https://your-api.com/openapi.json
          # cli-version: 1.1.3  # pin for reproducible CI
